import HeroBlock from '@/components/home/hero-block';
import NeedsBlock from '@/components/home/needs-block';

const Home = () => {
    return (
        <>
            <HeroBlock />
            <NeedsBlock />
        </>
    );
};

export default Home;
